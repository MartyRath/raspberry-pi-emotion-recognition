import BlynkLib

BLYNK_AUTH = 'Hm703ShkXO-pmualjhD1E6xaWBoDwjDH'
blynk = BlynkLib.Blynk(BLYNK_AUTH)

def run_blynk():
    blynk.run()

def write_to_virtual_pin(pin, value):
    blynk.virtual_write(pin, value)